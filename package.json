{
  "name": "footy-frontend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "rm -rf dev/* && parcel src/index.html --no-source-maps --no-cache --out-dir dev",
    "build": "rm -rf dist/* && parcel build src/index.html --no-source-maps --no-cache --public-url ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "^1.12.5",
    "sass": "^1.32.13"
  }
}
